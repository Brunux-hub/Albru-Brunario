# Archivo de ejemplo para variables de entorno
# Copia este archivo como .env y ajusta los valores según tu entorno

# === CONFIGURACIÓN DE BASE DE DATOS ===
# Contraseña del usuario root de MySQL (para contenedor DB local)
DB_ROOT_PASSWORD=

# Configuración principal de la base de datos
DB_NAME=albru
DB_USER=albru
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306
DB_CONN_LIMIT=10

# === PUERTOS DE SERVICIOS ===
# Puerto del backend API
BACKEND_PORT=3001

# Puerto de MySQL (mapeo host:contenedor)
MYSQL_PORT=3307

# Puerto de Adminer (administrador web de BD)
ADMINER_PORT=8080

# === SEGURIDAD ===
# JWT Secret para autenticación (OBLIGATORIO - usar clave larga y segura)
JWT_SECRET=

# === CONFIGURACIÓN DE APLICACIÓN ===
# Entorno: development, production, test
NODE_ENV=production

# URL del frontend (para CORS y configuración)
FRONTEND_URL=http://localhost