# Archivo de ejemplo para variables de entorno de Docker
# Copia este archivo a .env y ajusta los valores según necesites

# Contraseña del usuario root de MySQL
DB_ROOT_PASSWORD=root_password_here

# Configuración de la base de datos
DB_NAME=albru
DB_USER=albru
DB_PASSWORD=albru12345

# Puertos (opcional, por defecto usa los del docker-compose.yml)
BACKEND_PORT=3001
MYSQL_PORT=3306
ADMINER_PORT=8080

# JWT Secret para autenticación (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET=albru_jwt_secret_key_2025_secure_production

# Configuración de entorno
NODE_ENV=production