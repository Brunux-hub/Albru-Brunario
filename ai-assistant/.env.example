# Configuración del Asistente de Código IA

# Modelo a usar por defecto
MODEL_NAME=qwen2.5-coder:32b

# Parámetros del modelo
TEMPERATURE=0.7
TOP_P=0.9
NUM_CTX=8192

# Rutas del proyecto
PROJECT_ROOT=..
BACKEND_PATH=../backend
FRONTEND_PATH=../src
DATABASE_PATH=../database

# Base de datos (opcional, para análisis de schema)
DB_HOST=localhost
DB_PORT=3308
DB_USER=albru
DB_PASSWORD=albru_pass
DB_NAME=albru

# Ollama (normalmente no necesitas cambiar esto)
OLLAMA_HOST=http://localhost:11434
